---
name: agent-instruction-hygiene
description: スキル(SKILL.md)・CLAUDE.md/AGENTS.md・タスクプロンプトを新規作成/改訂するときに使う。特に高性能モデル向けに、冗長な指示・過剰な確認要求・レシピ的すぎる手順を削ぎ落として書き直す判断基準を提供する。
---

# Agent Instruction Hygiene

モデル非依存の一般原則として整理したもの。特定モデル固有の話ではなく、
Claude含む高性能エージェント全般の指示設計に適用できる。

## Overview

モデルが賢くなるほど「モデルを縛る指示」は劣化コストの方が大きくなる。
これまで有効だった手取り足取りの指示・過剰な確認要求・全文読み込み強制は、
現行の高性能モデルには「context の無駄食い」「過剰に慎重になって手を止める」
「本来やって欲しい範囲より手前で止まる」という副作用を生む。

見直すべき対象は3種類:
1. スキル(SKILL.md)のdescriptionと本文
2. CLAUDE.md / AGENTS.md 相当の常時ロードされる指示
3. 個別タスクのプロンプト(完了条件・裁量範囲の指定)

## When to Use

- 新しいスキルを書く/既存スキルのdescriptionを見直すとき
- CLAUDE.md・AGENTS.mdなど「毎回読み込まれる指示ファイル」を棚卸しするとき
- 「前のモデルでは効いていた縛りが、逆に仕事を遅くしている」と感じるとき
- タスクを渡した後、モデルが本来任せたい範囲より早く手を止めて確認してくる、または逆に確認すべき場面で確認せず進めてしまうとき

## Core Pattern

### 1. スキルdescriptionは「いつ使うか」だけ、最短で

```
❌ Bad:  Postgresのスキーマmigrationを作成・検証する。DB、クエリ、モデル、
         永続化に関する作業全般で使う。
✅ Good: Postgresのスキーマmigrationを作成・検証する。migrationの追加/変更、
         またはそのロールアウトレビュー時に使う。
```
悪い例は「DBに触れたら常に発動」してしまう。descriptionは発火条件を絞り込むためにある。
複数スキルを詰め込むほどdescriptionは自動的に切り詰められるので、最初から短く書く。

### 2. Progressive disclosure — ルートは薄いルーターに

複数ワークフローを持つスキルは、ルートSKILL.mdに全部書かない。
ルートは「どの場合にどのファイルを見ればいいか」だけを示す索引にし、
詳細(重い手順・APIリファレンス)は補助ファイルに逃がす。
スキルを読むこと自体がcontextを消費し、compactionに近づき、
今のタスクに無関係な指示まで読み込ませてしまうため。

### 3. 手順を過剰にレシピ化しない

以前は「1,2,3...」と逐一書かないとモデルが迷走したが、
現行モデルは曖昧さ・文脈からの判断が効くようになっている。
過度に細かい手順は、状況が少し違うだけで的外れな指示として足を引っ張る。
「何をすべきか」の原則を示し、手順の細部はモデルの判断に委ねる方が結果が良くなる場合がある。

複数モデル/複数コントリビューターが使う共有スキルの場合は特に注意:
自分が主に使うモデル向けに強く最適化した指示が、他モデルには過剰制約になりうる。

### 4. CLAUDE.md/AGENTS.mdは「読ませる」より「道しるべ」に

```
❌ Bad:  編集前に必ず architecture.md, database.md, deployment.md を読むこと。
✅ Good: サービス境界の変更は architecture.md、スキーマ変更は database.md、
         デプロイ準備時は deployment.md を参照。
```
「毎回全部読め」はtypo修正のような軽微な変更にもcontextを浪費させる。
どのドキュメントがどの状況で必要かを紐付け、ドキュメント自体も常に最新化しておく。

同様に「テストを必ず実行しろ」等、現行モデルが自発的にやる行動を
指示し続けると冗長な検証が増えるだけの場合がある。実際の挙動を見て要否を判断する。

### 5. 安全な定型作業は事前に許可を渡す

現行の高性能モデルは「危険なら聞く」判断力が上がっている一方、
慎重すぎて安全な作業まで逐一確認を求めてくることがある。
安全と分かっている繰り返し作業は、CLAUDE.md/AGENTS.mdやタスクプロンプトで
事前に許可を明記し、都度の確認を省かせる。

```
例: 「ローカルテストは使い捨てfixtureのみを使い本番アクセスは無い。
　　  実行し、今回の変更起因の失敗を修正し、影響範囲のテストを
　　  再実行してよい。各ステップでの承認確認は不要。」
```

逆に、以前のモデル向けに「必ず確認してから進めること」と強く書いた文言が
残っている場合、現行モデルには過剰制約として効いてしまうことがある。
モデル世代を更新したら、この手の「縛り」文言も合わせて見直す。

### 6. タスク開始前に「完了」の定義を明記する

現行モデルは、最初の実装ができた時点で一旦レビューを求めて止まることがある
(以前のモデルが延々と作業を続けたのとは逆の傾向)。
「動くところまで実装し、実際に動かして確認し、失敗を直すところまでが完了」
なのか、「最初の実装ができた時点でレビューに戻ってほしい」のかを、
タスクを投げる時点で明示する。指定が無いと、モデルは手前で止まりがちになる。
どこまで探索してほしいか(範囲と終了条件)も同様に明示する。

## Quick Reference

| 見直す対象 | チェック項目 |
|---|---|
| スキルdescription | 発火条件だけか？ワークフロー要約が混じっていないか？500字以内か？ |
| スキル本体構成 | 全ワークフローを1ファイルに詰めていないか？補助ファイルに分離できるか？ |
| 手順の書き方 | 逐一手順化しすぎて状況依存の判断を奪っていないか？ |
| CLAUDE.md/AGENTS.md | 「毎回全部読め」的指示が残っていないか？ドキュメントは最新か？ |
| 確認要求の文言 | 旧モデル向けの強い「必ず確認」文言が、安全な定型作業まで縛っていないか？ |
| タスクプロンプト | 完了条件(どこまでやったら終わりか)を明示しているか？ |

## Common Mistakes

- スキルが増えるほどdescriptionが自動的に短縮される → 最初から短く書かないと肝心な発火条件が削られる
- descriptionにワークフロー概要を書くと、モデルが本文を読まずdescriptionだけで判断してしまう
- 「常に確認してから進めること」は世代を跨いで使い回すと、新しいモデルには効きすぎて過剰に手が止まる原因になる
- 完了条件を書かずに「作って」とだけ投げると、最初の実装だけで止まってレビュー待ちになることがある
